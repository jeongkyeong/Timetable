<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a4f.myapplication">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>"

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NavActivity"
            android:label="Mypage"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".Register"
            android:label="Register">
        </activity>
            .<activity
        android:name=".EntireTTActivity"
        android:label="MyTimetabe">

    </activity>
        <activity
            android:name=".MakeTTActivity"
            android:label="MakeTimetable">

        </activity>
        <activity
            android:name=".Option"
            android:label="Option">

        </activity>
        <activity
            android:name=".MyAccount"
            android:label="MyAccount">
        </activity>
        <activity
            android:name=".EditProfile"
            android:label="EditProfile">
        </activity>
        <activity
            android:name=".MyTT"
            android:label="MyTT">
        </activity>
        <activity
            android:name=".MyTTHistory"
            android:label="MyTTHistory">
        </activity>


    </application>

</manifest>